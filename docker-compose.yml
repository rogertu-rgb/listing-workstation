services:
  listing-workstation:
    build: .
    container_name: listing-workstation
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - listing-workstation-data:/app/data

volumes:
  listing-workstation-data:
